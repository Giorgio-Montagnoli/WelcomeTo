@model List<WelcomeTo.Models.ViewModels.ProjectCard>

<div class="row" style="width:640px;height:250px;">
    @{
        foreach (var item in Model)
        {

            <div class="col-md-4">
                <img src="@item.imgUrl" class="img-responsive">
            </div>
        }
    }
</div>